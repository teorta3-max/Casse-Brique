#include <conio.h>
#include <stdio.h>

/*déplacement*/

if (input() == "q")
{
    posPlat_x = -1; // si input est égal à q la position de la platforme bouge vers la gauche
}
if (input() == "d")
{
    posPlat_x = +1; // inverse
}